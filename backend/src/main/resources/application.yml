server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:postgresql://dpg-d33j9bodl3ps738ugbd0-a.oregon-postgres.render.com:5432/spring_boot_db_u38p
    username: spring_boot_db_u38p_user
    password: pEgz9RyrgqmbNVtKJuYLi5Oqm35K3yP7
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  codec:
    max-in-memory-size: 10MB

cors:
  allowed-origins: "*"
  allowed-methods: "*"
  allowed-headers: "*"
  exposed-headers: "*"

management:
  endpoints:
    web:
      exposure:
        include: "health,info"

aws:
  region: us-east-2
  s3:
    mock: false
    buckets:
      customer: fs-nikenpatel-customer-test
